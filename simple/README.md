# 模板说明

这两个模板是最简单基础的配置，主要用途是测试服务端和客户端是否可连。

## config.json

这个文件是服务端的配置文件，里面需要的修改有：

- id: 这个ID可以说是用户的唯一标识, 用`xray uuid`生成

## client-config.json

这个文件是客户端的配置文件，里面需要修改的有:

- id: 用户的唯一标识跟服务端的配置的ID要对齐
- address：你的服务器IP或者域名
